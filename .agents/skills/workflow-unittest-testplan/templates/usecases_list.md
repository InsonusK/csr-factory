# Template of usecases list
When you need confirm list of usecases for unittes. Use [template below](.agents/skills/workflow-unittest-testplan/templates/usecases_list.md#template) to show them for user confirmation.

## Template
```
- {Class}
    - {Method}
        - {Group}
            - WHEN_{condition}_THEN_{result}
                - {description}
                - {input}
                - {output}
                - {expected_result}
```
